Intent:
Create a dark-mode companion design board from the referenced approved light-mode board. This dark board will be used only to derive `.dark` shadcn theme variables and to verify dark-mode visual implementation.

Preserve:
- Same product, layout, information architecture, components, data density, hierarchy, and responsive behavior.
- Same desktop and mobile regions.
- Same selected records, interaction states, and visible product workflow.
- Same design-system strip structure.

Translate:
- Convert the light design system into a premium dark system with black/graphite surfaces, low-contrast borders, readable foregrounds, muted text, dark popovers, dark inputs, dark cards, and dark chart colors.
- Preserve the brand accent while adjusting it for dark contrast.
- Keep warning, success, risk, destructive, and approval states legible and visually balanced.
- Show `.dark`-ready token roles: background, foreground, card, popover, primary, secondary, muted, accent, destructive, border, input, ring, sidebar, and chart colors.

Constraints:
Do not redesign the app. Do not change the layout, product structure, or hierarchy. Do not introduce a generic dark dashboard aesthetic. The dark version should feel like the same product translated into dark mode.
