interface:
  display_name: "mock-to-ui"
  short_description: "Generate premium mocks first, then build the UI"
  default_prompt: "Use $mock-to-ui to generate a GPT Image design board for an app, then translate the approved mock into React + shadcn/ui with visual verification."
